name: Model Release

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: 3.8
      - name: Install tox and dependencies
        run: |
          python -m pip install tox
      - name: Model training
        run: |
          tox -e train
      - name: "Reading the version file"
        id: package_version
        uses: juliangruber/read-file-action@v1
        with:
          path: "./titantic_classification/VERSION"
      - name: Release model
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.TOKEN }}
        with:
          tag_name: ${{ steps.package_version.outputs.content }}
          release_name: Release ${{ steps.package_version.outputs.content }}
          draft: false
          prerelease: false
